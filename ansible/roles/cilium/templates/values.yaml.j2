kubeProxyReplacement: {{ cilium_kube_proxy_replacement | lower }}

k8sServiceHost: {{ k8s_service_host }}
k8sServicePort: {{ k8s_service_port }}

ipam:
  mode: kubernetes

routingMode: {{ cilium_routing_mode }}

autoDirectNodeRoutes: {{ cilium_auto_direct_node_routes | lower }}

ipv4NativeRoutingCIDR: {{ cilium_ipv4_native_routing_cidr }}

operator:
  replicas: 1

  prometheus:
    enabled: {{ cilium_prometheus_enabled | lower }}

hubble:
  enabled: {{ cilium_hubble_enabled | lower }}

  relay:
    enabled: {{ cilium_hubble_enabled | lower }}

  ui:
    enabled: false

prometheus:
  enabled: {{ cilium_prometheus_enabled | lower }}